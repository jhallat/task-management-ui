import { PrimaryLinkDirective } from '../primary-link.directive';

describe('PrimaryLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PrimaryLinkDirective();
    expect(directive).toBeTruthy();
  });
});
